version: "3.9"

services:
  api:
    container_name: pi-llm-api
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped